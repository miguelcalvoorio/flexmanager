import { ServicegroupPipe } from './servicegroup.pipe';

describe('ServicegroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicegroupPipe();
    expect(pipe).toBeTruthy();
  });
});
